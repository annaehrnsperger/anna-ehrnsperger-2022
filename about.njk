---
pageTitle: About
template: about
slug: about
---
{# ---
eleventyComputed:
  pageTitle: "{{ about.title[locale] }}"
template: about
permalink: "{{ about.slug[locale] | slug }}/index.html"
--- #}


<h1>About Page</h1>

<ul>
  {% for post in collections.posts %}
    {% set currentPost = post.data.post %}
    <li>
      <a href="/blog/{{ currentPost.slug }}">
        <p>{{ currentPost.title }}</p>
      </a>
    </li>
  {% endfor %}
</ul>

  <div class="h-screen w-full bg-black absolute inset-0 z-[-1]" data-pixi-container></div>
